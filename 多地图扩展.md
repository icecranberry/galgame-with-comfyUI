# 多地图扩展计划

> 状态：实施规划，尚未开发。依据 2026-09-12 当前工作区的小镇实现整理；文中的新增字段、接口和组件均为建议方案。
>
> 目标：保留现有小镇和存档，在同一个世界中逐步增加多个小镇，每个小镇拥有独立地图，玩家可以前往、返回，并继续原有生活与任务。

## 1. 整体方案

采用 **一个世界、多个小镇地图、一个玩家身份** 的结构。

- 当前小镇直接成为默认地图，保留原来的地图 ID、建筑、居民、账本和进度。
- 首期一个小镇对应一张地图，继续以现有 `town_maps.id` 作为地图身份，不为切换功能再建立一套平行的小镇数据模型。
- 每张地图分别保存布局、地点、出生点、居民归属、经营配置和局部运行状态。
- 玩家的钱包、背包、角色关系和长期解锁继续共享，切换地图不创建新玩家、不重新发放启动奖励。
- 首期通过镇名旁的“出行”入口选择小镇，免费、即时切换；后续再接入地图中的车站、渡口、山道等入口。
- 世界观配置 `world_setting_id` 是地图的内容与画风来源，不等于运行世界 `world_id`。切换地图不切换整个世界身份。

示例关系：

```text
当前世界 world_id
├─ 现有小镇：原地图 ID，默认落点与主要生活区
├─ 海边小镇：独立道路、码头、居民、商店
└─ 山间小镇：独立地形、工坊、居民、委托

共享：玩家、角色身份、钱袋、背包、关系、长期进度
分开：地图布局、地点、居民位置、店铺库存、当地机会
```

这些镇名只用于说明结构，不要求第一期同时制作三张完整地图。第一版以“原镇 + 一张内容精简但可游玩的新镇”验证闭环。

以后需要同一小镇的室内、郊外等多张地图时，再增加地图分组字段，如 `settlement_id`；切换仍使用 `mapId`，无须重新设计玩家位置与接口。

## 2. 当前代码具备什么，还缺什么

| 位置 | 当前事实 | 扩展时要做的事 |
|---|---|---|
| `agent-core/src/db/index.js` | 已有 `town_maps`；地点、NPC、相遇记录已有 `map_id` | 保留这些表，补齐其他空间数据的地图归属 |
| 同上 | `town_locations.key` 全库唯一，`town_characters` 和 `town_agent_state` 没有地图字段，玩家坐标没有地图归属 | 地点改为图内唯一；角色与玩家补上明确位置范围 |
| `agent-core/src/services/town/townMapService.js` | `getMapRow()`、`saveMap()` 都取 `ORDER BY id LIMIT 1` | 显式按 ID 读取，拆分创建与更新，禁止“保存第二镇”覆盖第一镇 |
| `agent-core/src/services/town/townService.js` | 模块内只有一份地图与角色运行状态；`reloadTown()` 重载这份状态 | 抽取按地图组织的运行上下文，切换不调用世界重置 |
| 同上 | `reloadTown()` 不增加世界 epoch；`resetWorld()` 才推进世界 epoch；加载时还存在全局相遇收尾等操作 | 区分地图重载、玩家旅行和世界重置，局部操作不能影响其他镇 |
| `agent-core/src/services/town/townBus.js` | 复用 `/api/stream`；地图更新含 `mapId`，多数场景事件只有世界范围 | 为移动、气泡、相遇等事件补齐地图范围 |
| `agent-core/src/services/town/townInitService.js` | 共用 `data/town/init-state.json`；启动与确认流程存在清理旧世界、素材、NPC、状态的代码 | 新建小镇必须走独立生成任务，限定目标地图与草稿范围 |
| `agent-core/src/services/town/townResponsibilityRuntime.js` | 自动补岗仍取第一张地图 | 按地图补岗，保证两镇的店铺、岗位与员工独立 |
| `agent-core/src/services/town/townEconomyRuntime.js` 等 | 世界身份、幂等命令、账本已存在；部分地点查询仅按 `key`，到场校验缺少 `mapId` | 复用经济底座，同时补齐地图、位置与业务归属校验 |
| `web-ui/src/stores/town.js` | 单份地图与状态，已有世界范围校验、请求序号及 SSE 订阅管理 | 扩展为可切换的当前地图状态，并拒绝旧地图的异步结果 |
| `web-ui/src/views/TownView.vue` | 地图、编辑器、人物互动集中在同一页面 | 保留一个页面，增加切图流程及统一状态清理 |
| `web-ui/src/town/renderers/` | 已有场景适配和渲染器更新、销毁能力 | 复用渲染器架构，补齐不同地图之间的缓存失效与资源释放 |

因此，不能只在页面上增加下拉框：真正的实施顺序是 **数据分图 → 运行与业务分图 → 切换协议 → 页面入口 → 新镇生成**。

## 3. 玩家怎么切换

### 3.1 第一版：从“出行”选择目的地

1. 玩家进入现有“小镇”页面，镇名旁显示“出行”。只有一张地图时不强行展示空列表，可在管理入口提示后续添加小镇。
2. 点击后打开“出行地图”暖纸面板，显示当前镇和可前往的小镇。
3. 每个目的地展示镇名、简短介绍、缩略图、主要特色和可进入状态；当前镇标为“身在此处”。
4. 选中一个目的地后，显示到达地点及当地简介；面板只保留一个主要按钮“前往这里”。
5. 点击后按钮进入 loading，页面暂停移动与新的现场互动，预载目标地图。
6. 服务端确认旅行成功后，切换地图、玩家落点、当地居民与场景信息；提示“已抵达海边小镇”。
7. 钱袋、背包和已有任务保留。属于其他镇的任务显示镇名，并提供“前往该镇”，不能在当前镇直接完成现场步骤。

切换成功后关闭出行面板，镜头跟随抵达位置。地图数据刷新不反复抢走玩家已经调整的镜头。

### 3.2 返回与再次进入

| 情况 | 落点规则 |
|---|---|
| 首次前往一张地图 | 使用该图配置的公共入口出生点 |
| 通过首期“出行”菜单返回曾到访的镇 | 优先恢复该图最近离开的位置；失效时使用入口附近安全格 |
| 页面刷新、断线重连、重启应用 | 恢复服务端记录的当前地图和权威位置，不重新发起旅行 |
| 后续通过车站、渡口等通行点旅行 | 使用目标入口规定的抵达点，不被历史位置覆盖 |
| 历史落点变成建筑、水面或越界 | 服务端按确定顺序搜索附近可站立且连通的格子，再尝试公共出生点 |
| 目标图没有任何合法落点 | 拒绝旅行，保留原地图与位置，提示修复地图入口 |

“返回原镇”也是一次正常旅行，不能复用一份过期页面快照代替服务端位置变更。

### 3.3 哪些状态允许离开

| 当前状态 | 行为 |
|---|---|
| 普通浏览、行走、无结算的闲聊 | 允许离开；在旅行成功后停止旧路径、收起对话与气泡 |
| 地图编辑有未保存修改 | 提供“保存后出发 / 放弃修改并出发 / 留在这里”；保存冲突时留在编辑界面 |
| 现场打工、不可中断的制作步骤、邀约进行中 | 展示具体原因与“返回当前事务”；允许取消的业务提供原有取消入口，完成后再出发 |
| 一笔扣款、交付或业务确认结果尚不明确 | 先查询原命令回执或用原幂等键重试，不能清空请求后重新发起 |
| 已接普通委托、等待交付的长期订单 | 可以离开，原进度和截止时间保留；需要到场的步骤必须回到对应镇 |
| 后台纯文案或图片生成 | 任务绑定原地图和业务对象继续处理，结果只更新它所属的内容，不占据新图对话框 |

具体业务是否可中断，由业务服务返回 `blockingReason` 和允许的操作，不由前端猜测。首期不增加跨镇自动跑腿、自动完成打工或自动携带所有居民的行为。

### 3.4 多标签页与多设备

当前是单玩家模型，因此服务端只有一个权威 `currentMapId`。

- 任一设备成功旅行后，其他正在游玩的页面收到玩家地图变更通知并同步过去。
- 未同步的页面立即停止提交现场操作，通过 `playerRevision` 拒绝旧页面命令。
- 地图编辑可以保留自己的 `editingMapId` 草稿；其他设备旅行不把 A 图草稿改写到 B 图。此时暂停编辑并把草稿从游玩渲染中移出，显示“玩家已前往另一小镇”，编辑保存仍针对原图。继续编辑 A 时进入明确的编辑预览，不能把 A 的编辑图层叠到 B 的现场。
- 管理员预览其他地图是只读预览，不改变玩家当前位置，也不能在预览图内交易、打工或交任务。

首期不提供“每个浏览器标签页各控制一个玩家”的语义。

## 4. UI 怎样融入现有小镇

以 `docs/design-system.md` 和点击 NPC 打开的 `TownDialogueStage.vue` 为直接参考。

- 出行列表复用 `TownPaperPanel.vue` 的暖纸生活面板，内容呈现为旅行册、路线牌或目的地条目，避免后台管理卡片堆叠。
- 后续点击站务员、船夫、向导时，复用 `TownDialogueStage.vue`，让角色介绍目的地，再呈现“出发 / 再想想”等游戏选项。
- 必须使用标准弹窗时复用 `LinsheModal.vue`，不自绘遮罩；避免把多个完整面板外壳重复嵌套。
- 普通操作使用 `LinsheButton`；确需搜索、选择和开关时使用 `LinsheInput / LinsheSelect / LinsheSwitch`，不新增另一套皮肤。
- 生成中、不可用、当前所在等状态要有文字说明；失败反馈沿用现有 Toast，提供重试入口。
- 移动端保留原有小镇方向适配与安全区，列表可滚动，主要操作始终可达；面板点击不能穿透触发地图移动。
- 验收覆盖桌面、移动端、暖色和暗夜主题，对照 NPC 对话框、信箱、世界观与 Toast；配色和交互尺度使用现有 token。

## 5. 数据怎样分开，哪些继续共享

### 5.1 作用域约定

统一采用以下身份，不再用“当前第一张地图”作为隐式上下文：

| 标识 | 含义 | 何时变化 |
|---|---|---|
| `worldId` | 当前存档的世界身份 | 新建独立世界时 |
| `worldEpoch` | 世界整体重置的代数 | 明确重置整个世界时；旅行不变 |
| `mapId` | 地图永久身份 | 去另一张地图时 |
| `mapEpoch`（新增） | 一张地图被整体重建的代数 | 重建该地图时；旅行、普通保存和运行时重载不变 |
| `mapVersion` | 地图布局版本，对应现有 `version` | 保存地图、修改影响地图的内容时递增 |
| `playerRevision`（新增） | 玩家所在场景的修订号 | 成功旅行、强制迁移等场景归属变化时递增；普通移动沿用现有移动修订机制 |

地图范围使用 `{ worldId, worldEpoch, mapId, mapEpoch }`；编辑另带 `expectedMapVersion`，玩家现场命令另带 `expectedPlayerRevision`。服务端根据登录身份解析玩家，不能相信客户端传入的玩家归属。

地图内对象 ID 可以重复，例如 A 图与 B 图都有对象 `1`；引用必须包含 `mapId`。数据库地点 ID 和角色身份仍保持原有稳定 ID。

### 5.2 建议调整的表

| 表 / 数据 | 调整方案 |
|---|---|
| `town_maps` | 增加 `world_id`、图内稳定名称键 `map_key`、`description`、`status`、`sort_order`、`thumbnail_path`、`spawn_x/y`、`map_epoch`；保留现有布局、尺寸、世界观和版本字段；`UNIQUE(world_id, map_key)` |
| `town_world_state` | 增加 `default_map_id`，指向明确的默认镇；不另存全局“所有人当前看的图” |
| `town_locations` | 将 `UNIQUE(key)` 改为 `UNIQUE(map_id, key)`，保留旧地点 ID；业务优先存 `location_id`，文字 key 只在明确 map 范围内解析 |
| `town_npcs` | 保留 `map_id` 作为居住 / 工作归属；查询、创建、补岗、工作地点与住宅校验全部按图执行 |
| `town_characters` | 增加 `home_map_id`；沿用一名角色一份入住绑定，住宅必须属于该图 |
| `town_players` | 增加 `current_map_id`、`player_revision`；现有 `grid_x/y` 只解释为这张当前图上的位置 |
| `town_agent_state` | 增加 `map_id`、`map_epoch`，继续以唯一 `agent_key` 保存实体的当前状态，避免同一个角色同时出现在两镇 |
| 新增 `town_player_map_visits` | 保存玩家每张图的最近离开坐标和访问时间，唯一键 `(player_id, map_id)`；仅作返回落点参考，不是另一份实时玩家状态 |
| 新增 `town_travel_requests` | 保存请求键、规范化请求摘要、旅行回执及前后场景修订号，用于重试与结果查询；唯一键包含世界和玩家身份 |
| `town_business_slices` | 从每世界每 epoch 一份配置，调整为每地图每 map epoch 一份；迁移旧配置时保留账户与库存引用 |
| 任务、订单、服务、邀约、行动、事件 | 增加或在持久化结构内明确写入 `map_id/map_epoch`，历史记录保留发生地；不能继续依赖裸 `location_key` 或客户端当前地图 |
| `town_simulation_state` | 记录当前动作对应地图；已有按 actor 唯一的状态可以保留，但离开、迁移与动作恢复必须核对地图 |

为地图居民、地图地点、地图内未完成业务建立相应索引。外键不能单独表达“住宅与 NPC 属于同一地图”等规则时，在事务内统一校验，不能只依赖前端过滤。

地图状态建议为 `draft / generating / ready / failed / archived`。玩家只可进入 `ready`；生成失败重试只影响对应草稿。

### 5.3 共享与隔离规则

| 内容 | 规则 |
|---|---|
| 玩家与角色身份 | 全世界稳定，同一个角色不因去另一个镇而复制 |
| 钱包、背包、关系、个人经历 | 全世界共享；经历保留发生镇名与地点，历史镇名可留快照 |
| NPC 与店铺 | NPC 有明确归属；各镇店铺的经营配置、账户、库存和订单独立 |
| 地点、作息、日程匹配 | 在角色所在图 / 目标图范围内匹配，同名“咖啡馆”不能跨图命中 |
| 普通任务与邀约 | 保留来源图和目标地点；首期不生成跨图多步骤任务，但离开不会清空已接任务 |
| 长期解锁、周任务冷却、领取上限 | 保持原有世界 / 玩家范围，不因切图、创建新图或重建地图重置 |
| 天气、时钟 | 首期继续共享同一世界时钟与现有天气来源；地图可有景观差异，区域天气后续扩展 |
| 素材 | 共用现有素材库，地图显式引用素材 ID；加载时按实际引用与在场角色组织清单 |

新地图允许出现同名建筑，但不能因此复用旧镇商户的库存。现有 `delivery:business:cafe` 等业务身份要映射到实际经营实体；新增商户使用含 `mapId` 或稳定商户 ID 的名称键。

**旧账户、库存、订单、预留资金与历史幂等键原样保留**，通过经营配置建立映射，不能批量改名造成重复建账。新镇的有限启动资源从既有世界经济规则分配，世界额度不随地图数量放大，也不能每次打开地图就补一笔钱。

### 5.4 角色什么时候去另一张图

第一版：玩家可以旅行，NPC 留在自己的镇，已有聊天角色只能选择一个入住镇。

- 管理面板为入住角色提供“居住小镇 + 住宅”选择，切换镇后住宅列表同步过滤。
- 跨镇调整入住属于“迁居”，与玩家出行分开处理；存在进行中现场业务或邀约时先按业务规则结束或改期。
- 迁居保留角色身份、关系和余额，只更新归属、位置、未来地点引用；同一时刻只能属于一个现场。
- NPC 被邀请为聊天角色后继续沿用同一规范 actor。已有关联角色入住绑定时，以该绑定作为常住归属来源；迁居事务同步关联 NPC 的地图字段，并结束或明确解除不再适用的原岗位 / 住宅绑定。成员加载与补岗按规范 actor 去重，不能让 A 镇仍以 NPC 身份排班、B 镇又以角色身份入住。
- 首期不自动让角色随玩家旅行；后续同行功能以明确参与者名单和同一旅行事务迁移，不能在新图复制角色快照。

## 6. 后端如何实现可靠切换

### 6.1 先把地图服务改成显式目标

建议将现有接口职责拆为：

```text
listMaps(worldId)                        列出目录与状态，不返回全量图层
getMapRow(mapId)                         按 ID 读取指定地图
getMapPayload(mapId)                     指定图的图层、地点、素材清单
createMap(input)                        只创建新地图
updateMap(mapId, input, expectedVersion) 只更新指定地图
reloadMap(mapId)                        只重建指定图的运行上下文
travelPlayer(command)                   校验并提交一次玩家旅行
```

这里是建议签名，落地时可沿用现有命名习惯，但必须去掉“查询第一张地图”和“有第一张就覆盖”的逻辑。

旧接口兼容期可保留明确的默认图只读入口。**当数据库已有多张 ready 地图时，缺少 map scope 的旧版写请求应拒绝并提示刷新客户端**；不能悄悄写入玩家刚刚切到的图。

### 6.2 建议 API

以下均以 `/api/town` 为前缀；新增的业务字段也要同步进入现有 `townLife`、邀约、订单、任务等客户端封装。

| 接口 | 职责 |
|---|---|
| `GET /maps` | 返回目录、默认图、玩家当前图与 `playerRevision` |
| `GET /maps/:mapId` | 获取指定图渲染载荷及地图版本 |
| `GET /maps/:mapId/state` | 获取指定图权威场景快照，包含事件游标与地图范围 |
| `POST /maps` | 创建草稿，返回新地图 ID；管理操作不自动搬动玩家 |
| `PUT /maps/:mapId` | 携带 `expectedMapVersion` 保存；版本冲突返回 `409` |
| `POST /maps/:mapId/init-jobs` | 为草稿创建生成任务 |
| `GET /init-jobs/:jobId` | 查询该任务状态，替代全局单一向导状态 |
| `POST /init-jobs/:jobId/confirm` | 幂等发布目标图，返回 `ready` 状态 |
| `POST /travel` | 提交玩家从 A 到 B 的旅行 |
| `GET /travel/:requestId` | 查询属于当前玩家的旅行回执，处理超时与断线 |
| 现有 `/player/move`、互动、交易、任务等命令 | 补地图范围与场景修订号；地图坐标类命令另核对布局版本 |

首期菜单出行请求示例（ID 与数值仅为示意）：

```json
{
  "requestId": "travel-unique-request-id",
  "worldId": "existing-world-id",
  "worldEpoch": 1,
  "fromMapId": 7,
  "fromMapEpoch": 1,
  "targetMapId": 12,
  "targetMapEpoch": 1,
  "expectedTargetMapVersion": 3,
  "expectedPlayerRevision": 8,
  "entryMode": "menu"
}
```

成功回执示例：

```json
{
  "ok": true,
  "requestId": "travel-unique-request-id",
  "worldId": "existing-world-id",
  "worldEpoch": 1,
  "mapId": 12,
  "mapEpoch": 1,
  "mapVersion": 3,
  "playerRevision": 9,
  "position": { "x": 8, "y": 16 }
}
```

玩家身份从服务端会话获得，落点由服务端计算。菜单入口首期为所有可访问的 ready 地图；后续 `entryMode: "portal"` 增加 `portalId`，服务端检查玩家确实在入口附近、线路有效及目的地匹配，不能相信客户端任意指定入口。

同一 `requestId`、同一请求重试返回原回执；同一键换参数返回冲突。回执只能证明该次旅行发生过，客户端仍需核对当前 `playerRevision`，避免较早的成功回执覆盖后续旅行。

### 6.3 一次切换的执行顺序

```text
玩家选择 B 镇
  → 前端检查编辑草稿与未完成命令
  → 读取 B 地图、预载必要素材，绑定本次 switchToken
  → POST /travel，提交 A→B 和预期版本
  → 服务端事务再次校验，写入玩家位置、修订号与旅行回执
  → 提交成功，发布玩家换图事件并更新内存上下文
  → 客户端获取 B 的权威快照，与已缓冲事件对齐
  → 原子替换画面与状态，镜头对准抵达点
  → 恢复操作，记录本次访问
```

关键约束：

1. 网络请求、素材预载等耗时工作放在事务前；事务内不等待 LLM、生图或资源下载。
2. 进入玩家移动、tick 与旅行共用的串行临界区，将源图路径推进到统一服务端时间，取得当前权威停靠格并暂停继续推进；当前实现行走中可能只有内存位置，不能直接拿数据库旧坐标作为离开点。随后在事务内重读玩家当前图与 `playerRevision`，验证世界、来源图、目标图代数和目标布局版本，检查业务阻塞及安全落点。
3. 保存 A 图最近离开坐标，更新玩家当前图和坐标、清除旧移动路径、更新唯一玩家运行状态，递增场景修订号；这些数据与旅行回执一同提交。
4. 同时发生的两次旅行只允许一个满足预期修订号；后一个返回 `PLAYER_SCENE_CHANGED`，携带当前场景信息供同步。
5. 移动、接单、交易等现场命令也要在最终写入事务中检查玩家场景；先读取通过、之后异步生成完成再提交的命令必须重新检查。
6. 提交后通过现有事件机制发布持久化的玩家换图通知；进程在广播前崩溃也能靠玩家记录和旅行回执恢复。
7. 内存运行时不是成功的唯一依据。提交后内存更新失败时从数据库重建 B 图，不把旅行回执伪报为未发生。
8. 事务拒绝或回滚时释放玩家临界区，恢复源图的正常运行；旧路径是否继续按现有移动规则处理，不残留永久操作锁。

### 6.4 失败时怎么处理

| 失败点 | 恢复行为 |
|---|---|
| 预载失败、提交前取消 | 尚未改变玩家场景，保留 A；允许重试或关闭面板 |
| 目标未 ready / 已归档 / 无落点 | 服务端拒绝，留在 A，展示具体原因 |
| 预载期间 B 被编辑或重建 | 返回版本冲突，重新加载 B 再提交，不沿用旧落点 |
| 请求超时，是否提交未知 | 保持操作锁定，查回执或用原请求键重试；不自动生成第二次旅行 |
| 旅行已提交，但 B 的渲染失败 | 玩家仍在 B；提供重新载入目标场景、重试 HD-2D 的恢复入口；要返回 A 必须提交一次新旅行。完整 Canvas 降级如需提供，应另行实现并验证 |
| 快速点击 B、C | 同时最多一个已提交但结果未决的旅行；提交前预载可取消或替换，提交后先确认结果，再处理 C |
| 刷新后本地记忆与服务端不同 | 以服务端为准；本地只保存相机等偏好，不覆盖权威位置 |

## 7. 运行时、事件与缓存如何避免串图

### 7.1 后端运行上下文

将 `townService.js` 中的单一地图状态逐步抽取为 `TownMapRuntime`，由一个管理器按 `mapId` 获取。命名是建议，不要求一次重写整个服务。

每份上下文包含该图的地图数据、阻挡网格、地点匹配器、实体位置、寻路、相遇与局部缓存。世界时钟、身份注册表、钱包账本和公共调度器保持世界级。

- `getTownActorPosition()` 返回 `mapId/mapEpoch`；到场、距离、同地点、居民互动都先检查同图，再比较坐标或地点。
- `loadState()`、相遇收尾、角色成员同步、状态持久化、住宅绑定与 NPC 补岗均按地图限定范围。
- `buildLocationMatcher()` 继续作为纯函数复用，但输入只能是指定图的地点；自由文本地点不明确时返回未匹配，不从全世界挑第一个“咖啡馆”。
- 异步聊天与服务生成捕获原始 `{ scope, actorId, sessionId }`，完成时核对原业务状态，不读取“现在的活动地图”决定写入目标。
- tick、定时持久化和移动回调写玩家状态时也要核对地图范围与 `playerRevision`；旅行提交后撤销源图对玩家的更新资格，防止 A 图晚到的保存把玩家写回旧坐标。
- 缓存键包含地图 ID 与必要版本，禁止仅以 `cafe`、`npc_1`、`version=1` 作为跨图缓存身份。

### 7.2 当前图与后台地图

第一版只对玩家所在图运行完整实时移动、近距离判断和现场表现。其他图保留持久状态，以较低成本维护业务截止、生产、预约和投递。

- 不为每张图复制一套无上限的定时器、LLM 队列和连接；由统一调度器分配工作。
- 离开某镇不冻结资金预留、任务截止或已经承诺的交付，也不立即取消所有当地业务。
- 重新进入时按照当前时间重建当地居民的合理活动与位置，不逐帧回放离线移动。
- 复用 `townClock.js` 的有界追赶思想；普通活动补算与经济结算分开，不能把“离线时间”视为已经完成现场出勤。
- 需要真实到场的报酬必须依赖已有业务证据。首期不支持的后台现场行为保持等待或按原规则过期，不凭模拟位置发工资。
- 地图运行上下文可以按最近使用情况释放，正在履行的业务由独立调度继续处理；释放前保存必要游标和状态。

地图数量增长时，再依据 CPU、内存、同时进行的业务量决定后台批次和热图上限；不在规划阶段声称能无限承载地图。

### 7.3 SSE 与快照

继续复用 `/api/stream`，不为每张地图新开一个 SSE 连接。

| 事件 | 范围与处理 |
|---|---|
| 移动、气泡、相遇、当地状态 | 必须带地图范围，只进入当前图场景 |
| `town_map_updated` | 带 `mapId/mapEpoch/mapVersion`；当前图重载，其他图只标缓存失效 |
| 新增 `town_player_map_changed` | 玩家级事件，携带新地图与 `playerRevision`，即使来自非当前图也必须处理 |
| 地图创建、归档、发布 | 更新目的地目录，不自动抢占当前画面 |
| `town_init_progress` | 增加 `jobId/mapId` 与任务代数，只更新对应向导 |
| 钱包、物品、长期解锁 | 继续按世界 / 玩家范围处理，不因地图过滤而丢失 |
| 素材更新 | 按素材 ID 与引用关系失效，不假设只影响当前图 |

为场景快照和增量事件提供一致的事件游标，例如 `{ runtimeId, seq }`：每图运行实例内 `seq` 单调增加，实例重建产生新 `runtimeId`。它用于客户端排序与重同步，不代替持久业务日志或 `mapEpoch`。

切换期间先缓冲目标图事件，再获取快照；只应用同一范围、同一运行实例且序号晚于快照的事件。实例变化、事件缺口或缓冲溢出时重新取快照。这样既不会漏掉加载期间的 NPC 移动，也不会把 A 图延迟气泡放到 B 图。

玩家换图事件另按世界范围与 `playerRevision` 排序，只接受比已知场景更新的修订号，不套当前图过滤。SSE 重连先读取服务端玩家当前图与修订号，再载入对应场景快照；断线期间另一设备已经旅行时，不能只重载本地记住的旧图。新的世界代数到来时先清空旧场景范围，再以该代数的玩家快照建立基线。

### 7.4 前端状态拆分

`stores/town.js` 建议增加：

```text
地图目录：maps / defaultMapId
权威场景：activeMapId / mapEpoch / playerRevision
加载流程：switchState / switchToken / pendingTravelRequest
场景缓存：按 worldId + worldEpoch + mapId + mapEpoch + mapVersion 存储
地图偏好：每图相机、缩放、跟随状态
```

`activeMapId` 表示客户端已确认的玩家地图；选择目的地只改变 `targetMapId`，不能提前改写当前场景。

`TownView.vue` 将切换整理成单一入口。提交成功并接好新快照后，统一清理旧图选中 NPC、建筑、地点、寻路线、气泡、相遇、对话、就近检测、拾取缓存和旧异步回调，再挂接新图。

- 编辑器固定保存 `editingMapId + editingMapEpoch + baseVersion`，不能保存时才从 store 读取地图 ID。
- 进入切换流程时清空 WASD 按键、长按移动计时器与拖拽状态；恢复操作后等待新的输入，不能延续旧图尚未松开的按键。
- `townLife.js`、`townAppointments.js` 等持久化未决命令保存完整业务范围与原幂等键。切图后不能把 A 图未决命令的参数替换成 B 图，也不能无故删掉该命令。
- 旧请求的结果若不匹配 `switchToken` 或目标地图，只更新其归属缓存 / 历史记录，不覆盖当前场景。
- 地图载荷与状态快照的完整范围、布局版本必须对齐后才展示；获取两者期间发生编辑或重建时重新取数，不能只判断“地图不为空”。
- 新图与旧图即使尺寸、版本号或对象 ID 相同，也必须触发场景更换；相机、地形、外围景物、天气效果和对象实例不能仅凭版本号复用。
- 通过 `TownSceneAdapter` 统一 Canvas 与 HD-2D 的切换行为；必要时销毁重建。共用纹理按素材引用管理，释放旧图对象与渲染目标时不误删新图仍在用的资源。
- 信箱、任务卡等已有深链接补上来源 `mapId`；打开其他镇的任务时先展示来源与“前往该镇”，不因解析链接自动旅行、接单或提交现场动作。

## 8. 后续如何新增一座小镇

新镇创建和玩家旅行是两个独立流程。生成过程中，玩家可以继续在原镇生活。

### 8.1 内容制作流程

1. 管理面板选择“新增小镇”，创建一个独立 `draft` 地图 ID。
2. 配置镇名、简介、世界观来源、地图规格、居民数量和主题特色；默认继承现有世界风格，可调整内容设定。
3. 选择复用素材与需要新增的素材。已有草地、道路、树木、普通建筑可直接复用，不要求每镇重新生成整套资源。
4. 生成蓝图、居民草案和地图布局；每个任务持有固定 `jobId + targetMapId`。
5. 在草稿预览里检查道路、住宅、建筑入口、公共出生点与主要地点的可达性；预览不替换当前游玩地图。
6. 确认后，在目标图事务中保存地图、地点、居民归属和经营绑定；外部生图与 LLM 任务已在此前完成。
7. 验证可游玩条件后将状态改为 `ready`，目录出现该镇，并提供“现在前往”。发布本身不搬动玩家。

空存档的首镇是初始化例外：第一张地图发布时，在同一事务中设置缺失的 `default_map_id`，为尚无地图的玩家初始化当前图、安全坐标与场景修订号，并写入对应运行状态。该步骤只对未初始化玩家执行且可幂等重试；后续新镇发布不改变玩家位置。正常 `/travel` 因而总能要求有效来源地图，无须用伪造的来源 ID 完成首次进入。

地图至少应具备一个安全公共落点、连通的可玩区域与可识别的主要入口；生成 NPC 时，住宅和工作地点必须存在于该图。没有商店的景观小镇不强行套齐咖啡馆、工坊等全套经济设施，缺少必需业务承载点时明确标为待配置。

### 8.2 初始化向导需要改造的地方

- 将全局 `init-state.json` 改为任务隔离存储，如 `data/town/init-jobs/<jobId>/state.json`，记录目标地图、任务版本、已生成素材和取消状态。
- 现有单镇初始化与“新增小镇”显式区分模式；新增模式不得调用 `resetWorld()`，也不得执行全局 `DELETE FROM town_assets / town_npcs / town_agent_state`。
- 建议生成期间先保存居民草稿；若必须提前建档，标记为该任务的未发布数据，不参与实时模拟。
- 确认、取消、重试、恢复和组件卸载时的设置保存，都绑定固定任务与目标图；不能因玩家换图而保存到另一张地图。
- 废弃任务的素材只在确认没有任何地图、居民、草稿引用后清理，不能按“本次任务以外全部删除”处理。
- 素材查重不能只比较裸 `grass / cafe` 等 key。环境素材以素材集或任务命名空间识别，任务持有明确的 `key → assetId` 映射，小样、批量生成、布局生成和布局归一化全部使用该映射；跨镇复用通过明确素材 ID 选择。生成回调校验目标地图代数与任务状态，玩家 / 角色共用素材继续按实体身份管理。
- 每个任务确认幂等；反复点击“开镇”只发布同一张地图，不重复创建居民、店铺或启动资金。
- 如调整 LLM 蓝图 / 布局字段，按根目录 `AGENTS.md` 同步更新完整 JSON 示例、字段内容约束与解析校验；不让模型自行决定有效地图 ID、跨图引用或经济权限。
- 角色生图人格仍统一使用 `characterPersona.js`，多镇不能引入新的外观截取与拼接分支。

### 8.3 编辑、归档与素材维护

- 编辑当前镇或其他镇都必须明确目标地图，并带布局版本；被引用的地点删除仍检查订单、住宅、任务等关系。
- 第一版只提供归档，不提供随意硬删除地图。默认图、玩家所在图或仍有未完成现场业务的地图不能直接归档。
- 归档前先处理玩家迁移、居民归属和未完成业务；历史账本、经历与任务记录保留。
- 普通保存不增加 `mapEpoch`；整图重建才增加它，并按该图范围结束或迁移旧业务。整体世界重置继续是独立的显式操作。
- 素材管理显示所有地图引用。删除仍被引用的素材默认拒绝并列出使用位置；替换或清理时覆盖所有受影响地图并分别更新版本，不能只清理第一张图。

## 9. 后续如何增加车站、渡口与地图连通

菜单切换稳定后，再把“通行”能力接入地图中的角色和建筑。沿用《小镇功能类型扩展.md》中“通行”的概念，挂到已有互动能力流程，不新增一套独立 NPC 系统。

建议增加 `town_map_portals`，记录：

```text
id
source_map_id + source_location_id
target_map_id + target_spawn_x/y
display_name + enabled
访问条件的规则配置
```

一个记录表示一条有方向的线路。双向旅行配置两条线路，发布时验证两端入口与返回路径。

玩家操作：点击站务员 → 走到入口 → NPC 介绍目的地 → 选择“乘车前往” → 使用同一个 `travelPlayer()` 协议抵达目标站。

首期不收路费、不模拟行程时间。以后增加票价、车票、解锁、同行或旅途事件时，通过旅行服务扩展验证与幂等结算，不让 NPC 对话直接改玩家坐标。若引入收费，扣费、旅行记录与位置提交必须具有同一业务结果，避免扣钱未到达或重复扣钱。

地图外围纯展示带仍不参与寻路，通行点应放在真实地图可达格子内。只有线路已经接通时才显示可用入口；装饰性车站不能伪装成可前往的区域。

## 10. 旧存档如何升级

采用增量迁移，原镇作为默认图，**不要求用户重新生成小镇**。

1. 在测试副本上记录地图、地点、角色、账户、库存、订单、预留资金及任务的迁移前基线，再执行版本化迁移。
2. 没有地图的存档保持空状态，进入首镇创建流程；只有一张图时使用真实旧 ID，不假定它一定等于 `1`。
3. 给旧图补世界归属、稳定 `map_key` 与 `map_epoch=1`，验证 v2 图层及安全出生点后才设为 `ready` 并写入可用默认图。缺失或损坏 `layers_json` 的遗留图保留原数据，标记失败 / 待修复并提供管理入口，不能无条件开放旅行或初始化玩家落点。
4. 回填玩家当前图与运行状态。角色优先按住宅归属回填，旧 NPC 按已有 `map_id` 校验；确实只有一张图时，缺失归属可以回填到该图。
5. 将旧业务配置和任务地点绑定到原图，保留账户 ID、库存 ID、预留、订单、回执及历史 source key，禁止重新初始化经济。
6. 重建 `town_locations` 的唯一约束时保留全部旧地点 ID、外键关系、索引与触发器；按现有 SQLite 迁移规范执行表重建并检查外键完整性，不能只新增复合索引而留下原全局 UNIQUE。
7. 对历史 JSON 内的 `locationKey`、`businessKey`、任务步骤和作息引用，补齐可确定的地图范围；无法确定时标为待修复，不任意指向同名地点。
8. 如果旧库已经人工存在多张地图，优先按已有引用推导归属；多义数据单独报告，未完成修复前不开放多图写入，不能把所有记录强行归到第一张图。
9. 完成迁移后验证记录数量、身份引用、余额、预留、库存和未完成任务，重复执行迁移应无新增副作用。

发布先提供多地图功能开关，关闭时隐藏新入口，底层仍按已经迁移后的地图范围运行。回退界面功能不等于回退数据库结构；若旧版本无法识别多图，不能让它继续使用“更新第一张地图”的旧写逻辑。完整回退使用成套备份与对应程序版本。

## 11. 分阶段怎么执行

| 阶段 | 主要工作与文件 | 完成标准 |
|---|---|---|
| M0：梳理范围与迁移 | `db/index.js`、各 `town*Schema.js`；新增地图迁移模块，整理裸地点键及无地图查询 | 旧存档无损升级；测试库可保存 A、B 两图及各自同名 `cafe` |
| M1：地图与运行时隔离 | `townMapService.js`、`townService.js`、`townNpcService.js`、`townResponsibilityRuntime.js`、`townBus.js` | 保存、重载、补岗 A 不改 B；玩家与居民位置都有地图范围 |
| M2：业务隔离 | `townEconomyRuntime.js`、业务 / 任务 / 邀约 / 服务 / 行动 / 生产 / 地点匹配相关服务与 schema | 同坐标、同名地点不能跨图交易或履约；原钱袋与库存总量正确；后台截止不冻结 |
| M3：旅行协议与前端切换 | 新增旅行服务；`routes/town.js`、`stores/town.js`、API 封装、`TownView.vue`、渲染器；新增 `TownMapTravelPanel.vue`，适配现有任务 / 信箱链接 | 两张测试图可往返，刷新、多端、超时、连续点击和渲染失败均可恢复；手动旅行成功后可打开对应镇的任务手账 |
| M4：新增小镇与正式闭环 | `townInitService.js`、`TownInitWizard.vue`、`TownAdminPanel.vue`、素材引用管理 | 原镇存在时可以新建并发布第二镇；失败不伤旧镇；生成后可前往并回来继续任务 |
| M5：出行玩法扩展 | 通行点配置、NPC 通行能力、必要的地图管理 | 车站 / 渡口复用同一旅行协议，去程与返程都有效 |
| M6：规模与内容扩展 | 按实际地图数调度后台维护，优化引用素材与缓存；选择性增加同行、室内分图等 | 地图增加后内存、耗时和业务维护可控，资源不随反复切换持续增长 |

M0—M4 共同构成首个可发布版本。开发阶段可先用两张很小的固定地图验证隔离与切换，内容制作不必等待最终完整镇景，也不能用新 UI 已可点击作为底层隔离完成的依据。

首轮任务拆分顺序：

1. 增加迁移与明确 map scope，建立 A/B 图最小数据样本。
2. 完成地图 CRUD、地点唯一约束与归属校验。
3. 拆运行上下文，更新事件、位置、相遇和补岗逻辑。
4. 完成业务地图范围及旧经营身份映射，再开放任何跨图现场操作。
5. 实现旅行服务与回执恢复，之后接前端入口和状态清理。
6. 改造初始化向导，新建第二镇并验证旧存档不变。
7. 执行下方验收矩阵，达到要求后再开放多地图入口。

数据契约确定后，前端出行面板、草稿向导隔离和后端地图服务可以并行推进；地图身份、事务和经济迁移应由同一套契约约束，不能各自定义“当前小镇”。

## 12. 怎么验收

| 场景 | 必须满足 |
|---|---|
| 升级旧单镇存档 | 原地图与地点 ID 不变，原居民仍在，余额、库存、预留与任务进度不变 |
| 空存档与异常历史多图 | 空存档正常建首镇；多义历史引用被报告，不发生静默错误回填 |
| A、B 都叫“咖啡馆”，都位于 `(10,10)` | 查看、到场、消费、取货、交任务均只命中正确地图 |
| A 编辑保存、B 同时运营 | B 布局、角色、订单与 epoch 不受影响；A 编辑冲突有明确反馈 |
| A→B→A、刷新、后端重启 | 当前图与落点可靠恢复，旧图路径、气泡、对话和选中状态不残留 |
| 连点、两个标签页同时旅行 | 原子决定玩家最终位置，旧修订号命令失败，不产生重复玩家 |
| 请求已提交后断网、进程在广播前退出 | 用同一请求回执确认结果，恢复已提交地图，不发第二次相同旅行 |
| 预载或渲染失败 | 提交前留在原图；提交后恢复目标图，不能出现画面 A、命令 B 的可操作状态 |
| 延迟 SSE、快照期间移动、运行实例变化 | 通过范围与事件游标对齐，旧图事件丢弃，缺口重新取快照 |
| 打工中出行、交易结果未知、委托未完成 | 按规则阻止或保留；不丢预留、不重复结算、不在异地现场交付 |
| 离开一镇数小时再回来 | 业务截止与维护正确，居民活动合理重建，不凭离线时长发现场工资 |
| 创建第二镇失败、取消、重复确认 | 原镇正常游玩；只改对应草稿，不重复建居民、店铺和奖励 |
| 两图共用素材后替换或删除 | 所有引用可追踪，分别失效更新，其他地图不出现缺失资源 |
| 桌面 / 手机、暖色 / 暗夜、Canvas / HD-2D | 面板与 NPC 对话视觉一致，操作不穿透，切图后的相机和输入正常 |
| 连续往返多次 | 场景对象、纹理、监听器和定时器数量不持续增长；测量加载时间及内存峰值 |

测试应覆盖数据库迁移、作用域校验、旅行事务与幂等恢复；前端使用隔离的双地图样例测试异步竞态、SSE 与资源释放。沿用现有 Node 测试和前端夹具体系，性能以改造前基线和目标设备实测确定预算。

本文仅规划，未执行上述业务测试或修改运行代码；真正开发时按变更范围运行相关回归。更新说明 `web-ui/src/data/changelog.js` 仍只在用户明确要求时修改。

## 13. 第一版的交付边界

第一版完成后，用户应能实际做到：

- 继续使用原来的小镇。
- 在管理入口创建一座新的独立小镇。
- 通过“出行”在已开放的小镇之间往返。
- 刷新后留在正确的小镇，返回时恢复合理落点。
- 带着同一份钱袋、背包和关系继续生活，居民、商店、现场任务各归其镇。
- 在生成失败、切换超时或多端操作时恢复到一致状态。

连续无缝大世界、多人独立玩家、付费交通、角色自动巡游、跨镇产业链、室内多层地图和复杂世界选择均放在后续阶段。先把独立地图创建、可靠切换与存档隔离做完整，后续新增地图主要成为内容配置工作。
