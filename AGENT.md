「邻舍.EXE」整体采用一种温暖、柔和、低对比度的 Soft Warm UI 设计语言。

核心关键词：

**柔和、温暖、轻盈、克制、内容优先。**

### 色彩

使用暖灰 / 米灰作为页面背景，暖白作为内容容器背景，浅暖灰作为次级区域。

珊瑚色仅用于：

* 主操作
* Active / Selected 状态
* 重要链接
* 少量强调

不要大面积使用珊瑚色。

避免冷白、冷灰、高饱和色和典型 SaaS 蓝紫色。

### 容器

使用明显但柔和的圆弧矩形。

容器主要通过：

* 背景色差
* 圆角
* 极轻的环境阴影

建立层次。

尽量不要使用明显边框。

避免厚重阴影和 Card 套 Card。

### 间距

优先通过留白建立信息层级，而不是大量 Divider。

不同功能区域之间应该有自然呼吸感。

不要为了强调层级给每一个功能增加独立 Card。

### 文字

标题使用深暖灰，不使用纯黑。

正文使用中等深度灰。

辅助说明使用更浅的灰褐色。

通过字号、字重和留白建立层级，而不是依靠高对比度。

### 按钮

按钮必须具有明确的视觉权重：

主操作：
珊瑚色实心。

次操作：
浅暖色背景 / 柔和描边。

低权重操作：
透明或极浅背景。

不要让多个不同重要性的操作拥有相同的视觉重量。

### 控件

输入框、Segmented Control、按钮等都遵循统一的圆角体系。

控件应该融入整体容器，而不是产生强烈的“网页表单控件感”。

### 浮层 / Toast / Modal

所有浮层都应该像内容自然地从页面中“浮起来”，而不是像传统系统弹窗突然覆盖页面。

Toast 是最轻量的表现形式。

Modal / 图片确认窗口可以承载更多内容，但必须共享 Toast 的：

* 暖白背景
* 柔和圆角
* 低存在感阴影
* 低对比度文字
* 珊瑚色操作
* 留白优先
* 轻量按钮

因此大型窗口应该理解为：

**“Toast 设计语言的扩展，而不是另一套 Modal 设计。”**

### 最终视觉感受

用户不应该明显感觉到：

“这里有一个 Card”
“这里有一个 Modal”
“这里有一个 Form”

而应该感觉：

**“这是邻舍.EXE 里一块自然浮现出来的内容。”**

不要追求视觉效果本身。

优先保证：
**层级清晰 > 操作明确 > 空间舒服 > 装饰效果。**
