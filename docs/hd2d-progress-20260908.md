# HD2D 计划推进记录 · 2026-09-08

已于北京时间 2026-09-08 09:45 到达时间条件并停止。用户停止条件为09:45或主额度剩余5%，以先到者为准；本次按时间结束，未使用额度重置信用。所有子代理已结束，最终受测代码无后续变动，下一步入口见文末。此状态只表示本轮限时任务结束，不代表整个HD2D计划完成。

## 已落地与正在验收

- M0 [共享契约](hd2d-contracts.md)：稳定 world/epoch、actor、POI、时间与事务边界。新增增量身份/来源迁移，不删除原背包、人物或历史。
- 地图按 key 更新保留 id，PUT 透传 locations；显式清空与省略保留区分，删除引用不成立时事务回滚。向导确认也复用保存契约。
- NPC 邀请与 actor 绑定在同事务内完成，重复邀请返回原身份。镇上仅出现唯一 canonical actor；已关联角色沿用角色日程，NPC 精灵/住宅可回退。显式退住优先于旧 NPC 开关。
- 重建小镇先取消在途动作、释放租约、终止 NPC 待回复请求，再递增 epoch 并清场景；原角色/背包/actor 履历仍保留。旧场景相遇/摘要/气泡回包由装载代次隔离。
- NPC 到达以完整走过路径边为准；频繁状态请求不提前到达，寻路失败保留原位置并退避，不能瞬移算交货。作息与 NPC 对话共用指定时区及跨午夜窗口；LLM 气泡不覆盖本地活动。
- [M2 NPC 舞台](hd2d-m2-npc-frontend.md) 与 [角色舞台](hd2d-m2-character-chat.md)：统一设计组件、历史/输入隔离、移动端尺寸与晚回包保护已完成隔离验收；复用原角色历史/队列/流协议，新增镇内资格、距离校验和动态当面场景块。
- [M3 规则](hd2d-m3-pure-logic.md) 与 [动作/事件](hd2d-m3-actions.md)：有类型 AST、稳定选择、绝对冷却；持久动作/CAS/资源租约/日志/事件/outbox。`simulation=rules` 已接运行层，默认 `legacy`；完整服务集成继续验收。
- 道具来源与所有权：旧实例回填 `me/legacy_chest`；商城/服务生成状态不影响宝箱冷却/启动清理。玩家 use 效果与条件消耗同事务，玩家接口不操作别人的库存。
- M4 账本与配送：稳定商家账户、显式一次性初始预算、资金/库存预留、实际到达校验、30 邻币单次结算。钱包/委托/生活面板已接入并通过浏览器验收；过期/重建退回在途货物与预算。有限生产、双人真实工时、工资/采购/公共回流已通过真实模块链路及九十天实 API 审计；[仍不经济平衡](hd2d-m4-business-consumption-audit.md)。
- M5 工坊服务：报价/确认收费、有限阶段与回合、托管/材料投入/取消退款/终态结算、模型无效时本地完整流程。运行层/API/重建恢复、跨动作占用互斥与原背包效果通过完整模块测试；前端多视口、关闭重开、断网重试通过隔离浏览器验收，详见 [生活运行层](hd2d-life-runtime.md)。
- M6 首个经历适配器：配送/服务正式结算来源校验、参与者摘要、已关联角色原记忆写入、有限重试与日预算。没有自动外发表达，完整双向生态联动仍未完成。
- M6 免费回访：预约引擎、日程只读校验、收据/物品来源、CAS/幂等/冲突/重建契约、真实前往等候和页面已接入，并通过 HTTP/实际运行层/浏览器验收。居民活动面板展示行动缘由及已结算经历；原角色资料卡和轻量 NPC 舞台均有入口。
- M4 [有限基金保障](hd2d-m4-liquidity-policy.md)：默认关闭，真实委托前置、60 准备金、滚动与本镇累计上限；发行和订单预留原子提交。API/设置/生活状态及保存故障原子性已验证；管理页独立只读资金查询通过 SQLite query_only 验证。
- M7 [三栋体积原型](hd2d-m7-modular-volume.md)：显式 opt-in，保留旧图回退，日夜/遮挡/picking/GL/资源释放验证通过；[全镇压力与回退](hd2d-m7-stress-fallback.md)已完成桌面短稳态验证。[遮挡淡化](hd2d-m7-interaction-occlusion.md)已限制为玩家与明确交互对象，密集样本非焦点淡化建筑由 31 栋降为 0；六组短稳态复测结果与未解释的 CPU 变化见下文。随后补齐视口裁剪与人物透明留白检查，17 项定向测试及真实 WebGL 焦点浏览器通过，该边界修复尚未另做性能复测。
- M6 [私聊生活上下文](hd2d-m6-chat-life-context.md)：同一原聊天管线读取有界、只读的已结算经历与已接受预约，原日程冲突明确标记，不把预约说成已见面。
- M6 [记录投递维护](hd2d-m6-delivery-diagnostics.md)：管理页可查看经历/预约失败状态，手动重试沿用原事件，不重做交易；服务、HTTP和移动端入口已验证。
- M6 [信箱正式委托入口](hd2d-m6-mailbox-tasks.md)：只读卡片读取当前订单、账本预留和正式事件；关闭经济后仍可跟进已领取任务，关闭小镇隐藏卡片。点击只打开小镇生活面板，实际领取仍要求到达公告板。服务与真实 HTTP/runtime 33/33、API 7/7；真实信箱→小镇浏览器在375/1100宽度通过，分页跨世界替换和卸载晚响应也通过，写请求为0。
- M7 状态与恢复：统一 SSE 补齐状态变化转发；NPC 移动在 HTTP 读取期间不被旧快照覆盖，旧向导预览无法跨卸载/世界复活。store 与真实解析链路 10/10。四项面板晚响应/存储失败问题已修复并通过定向浏览器验证。
- M6 [日程中的预约记录](hd2d-m6-schedule-overlays.md)：只读共享 reader 接入角色抽屉与原私聊上下文；预约独立于基础日程显示，不表示已经见面。服务/路由与真实 ScheduleView/抽屉浏览器覆盖角色快速切换、晚成功/晚失败、跨世界刷新及明确北京时间展示。基础日程变更、预约过期通知与原每分钟/获焦刷新已接入，关闭抽屉不再读取；本页两项预览订阅卸载释放，慢挂载请求不再补注册旧监听。
- M6 [素材外观来源](hd2d-m6-appearance-signature.md)：生成输入统一捕获角色外观版本，旧素材标记未知，显式更新操作才重绘；生成期间换装会拒绝旧结果并保留原图。原 NPC 选项透传、并发保存生成配置、畸形历史凭证、删除关联角色和真实409路由已验证并纳入08:00后端检查点。管理页重开/换世界刷新，旧GET和POST回包不污染当前显示。
- M6 [原信箱回信生活记录](hd2d-m6-mailbox-life-context.md)：已有回信在模型调用前读取同一有界生活记录，检索等待期间换世界不会沿用旧记录；关闭功能、无记录或读取失败维持原回信。没有新增寄信/图片/调度触发。实际模块源码与共享 reader 的隔离组合22项通过。
- M7 [30/90天真实运行层回放](hd2d-m7-long-run.md)：正式寻路、工作证明、有限补助及持久结算参与正常/囤币双路径，各用新内存库重放一致。正常90日216配送/服务、200生产、发行540、完成未兑付0；囤币第21日起断货且无额外发行。30/90天服务与生产预留恢复无离线工资或补发，回拨暂停新发行。资源耗尽、冷却拒绝和收入指标限制均保留，未宣称经济平衡。

## 最终检查点与阶段验证

生产代码及测试已冻结，最后检查点如下；下方历史记录保留各阶段当时的范围，不累加为新的测试总数。

- 09:30:30启动的最终后端检查点 **663/663通过**，110.670秒，失败/取消/跳过/todo均0，退出码0。包括全部 `test/*.test.js`、原src两项和90天实际运行层回放，并发4；日志 `output/backend-checkpoint-20260908-093030-spec.log`。
- 09:35最终前端renderer/store/API单测 **59/59通过**，679ms；日志 `output/frontend-checkpoint-20260908-0935-spec.log`。隔离构建 **242 modules通过**，5.65秒，产物 `output/hd2d-checkpoint-0935/ui-build`，日志 `output/frontend-build-20260908-0935.log`；只有既有混合导入和chunk体积提示，未覆盖默认业务静态目录。
- 最后天气刷新浏览器使用真实store/API/事件dispatcher，覆盖跨小时、过期、系统时间回拨、限流、合并事件及卸载晚响应，**6 GET / 0 POST**，见 [ping刷新](hd2d-weather-ping-refresh.md)。
- 收尾 `git diff --check`通过；专题文档第一轮40份/138处本地链接目标存在（不校验段落锚点），后续新增咖啡馆交接引用另核对。`output/hd2d-final-tested-files.json`保存252个已修改/未跟踪的代码及测试文件SHA-256，关联最终三个日志；它不是提交或全仓快照。子代理与本轮fixture均已结束，原有开发服务未操作。

- 原有素材/A*/地点测试 20/20。
- 地点保存首批 8/8；后续向导与幂等追加测试随模块交接汇总。
- actor registry 12/12；道具兼容 10/10；权威 NPC 移动 2/2；跨午夜作息 2/2。
- 实际完整服务内存库：邀请/启停/重载/重建、NPC 无模型模板与角色历史入口保护通过。
- 独立动作/事件首批17/17；规则/事实/时钟首批15/15；后续模拟桥与完整运行层纳入后端检查点。
- NPC与角色浏览器 fixture、多视口、HD2D首批12/12及隔离构建通过；后续统一舞台与TownView集成见对应专题记录。
- 06:35 后端完整检查点：`node --test --test-reporter=spec --test-concurrency=4 test/*.test.js`，418/418 通过；日志在 `output/backend-checkpoint-20260908-0635-spec.log`。此后新增工作最终再汇总。
- 真实 `TownView`：生活/回访/居民近况入口、地图输入锁、epoch 关闭、三视口及触控竖屏通过。直接 renderer 50×50、80 对象、20/50/60 居民压力、20轮回退重建与资源清理通过；机器和样本限制见专门报告。
- `townRecoveryMatrix.test.js` 5/5：SQLite 重开、在途移动、服务故障恢复、生产离线不发工资、预约清租约、reset 九阶段故障回滚和最终资产守恒。
- 06:55 完整后端检查点：全部 `test/*.test.js` 加原路径寻路/地点匹配测试，451/451 通过，8.93 秒；日志 `output/backend-checkpoint-20260908-065511-spec.log`。06:48 的两项 HTTP 随机端口失败已定位为 fetch 禁用端口，测试监听器重选随机端口后定向十轮 240/240、完整检查点均通过。
- 工坊真实十秒超时独立验收 5/5，实际 10017.6ms，单请求、本地回退、连接/生成清理、托管与最终单物品交付均验证。设置、投递和真实 TownView 浏览器在只读资金接口接入后再次通过；未触及实际配置。
- `townBoundaryRuntime.test.js` 5/5：关闭功能后结束已有服务、地点删除/退住退款、预约地点删除解除占用及重复读取无新增写入。
- 07:08 完整后端检查点：489/489，8.81秒，日志 `output/backend-checkpoint-20260908-070828-spec.log`。07:10 前端隔离构建240 modules通过，输出 `output/hd2d-checkpoint-0710/ui-build`，仅原有混合导入和chunk体积提示。
- 前端 renderer/store/API 全部现有单测48/48。真实TownView同页卸载20轮（Canvas/HD各10）资源与订阅配平；慢state/map卸载后新增调度由3/99降为0，图片未返回时等待也能结束。此证据不外推完整JS堆、驱动显存或Android。
- 最新遮挡shader六组桌面短稳态样本：20/50/60居民、low/standard各180帧；60 standard帧间隔p95为16.8ms，但CPU p95由历史8.2升至14.1ms，保留该变化且不凭跨时段数据归因。50 standard当前CPU p95为7.7ms；GL错误与longtask均0。原始数据及限制见压力报告。
- 07:41 前端 renderer/store/API 单测53/53；随后 Admin 外观浏览器覆盖真实409、普通503、中途关闭再开、旧GET与生成POST晚返回，10次显式mock POST，无自动追加生成。新增列表刷新在每次打开时执行，生成中保持禁用，完成后只读取当前列表。
- 08:00:48 后端完整检查点563/563通过，108.04秒，包含长期回放；日志 `output/backend-checkpoint-20260908-080048-spec.log`。07:59:15检查点主动中止以补齐真实sprites路由错误契约，部分日志不作完整验收结果。当时前端56/56，日志 `output/frontend-checkpoint-20260908-0801-spec.log`。
- 08:34:52完整后端检查点606/606通过，108.496秒，取消/跳过/todo均0；包含B链、朋友圈/群聊生活记录、完整天气时间与实际启动/小镇runtime及90天回放。日志`output/backend-checkpoint-20260908-083452-spec.log`，并发4；此检查点覆盖上文08:00之后已交还的后端改动。
- 09:04:04完整后端检查点643/643通过，109.717秒，取消/跳过/todo均0；加入首次奇遇背景、天气故障降级/NPC时区、雨天有限避雨及真实业务优先级验证。日志`output/backend-checkpoint-20260908-090404-spec.log`，并发4，仍含90天实际回放。09:04隔离前端构建通过，输出`output/hd2d-checkpoint-0904/ui-build`。
- 09:19:27完整后端检查点661/661通过，112.354秒，取消/跳过/todo均0；加入天气配置source绑定、异步generation/revision保护、真实配置setter及调度器生命周期验证。日志`output/backend-checkpoint-20260908-091927-spec.log`，并发4且包含90天回放。
- [共享人格固定回归](hd2d-persona-regression.md)补齐9项指定入口测试，未修改 central 生产口径；[移动范围](hd2d-m7-movement-scope.md)为新客户端的坐标/方向移动附带world/epoch，旧请求不执行新世界移动。真实TownView验证生活面板随世界关闭、旧前往回包不残留错误；旧无scope客户端仍按兼容契约处理。

固定发型卡切片已接通：[B链工坊服务](hd2d-m6-bob-cut-service.md)复用既有 bob_cut 效果，以本epoch真实生产解锁，沿用30邻币/1材料/原取消退款和原背包手动使用。核心6项、旧服务32项及目录HTTP3项通过；经历/再次生产组合17项通过。独立真实模块整链1项确认授卡不换装、原背包use后生效24h、真实人格更新且旧素材needs_update，不增加免费回访。畸形历史结构曾导致目录TypeError，已改为返回锁定并保留真实SQL错误，修复与实际B链组合8项通过，已计入08:34完整后端检查点。

[原朋友圈生活记录](hd2d-m6-moments-life-context.md)已接主角色原生成入口，在模型调用前读取当前记录，梦境/自由模式排除。5项完整源码隔离测试通过；不新增发帖触发、模型或图片调用，也不把它当正式活动卡adapter。[原群聊](hd2d-m6-group-life-context.md)同类只读接入5项通过，与共享reader联跑23项通过，分别标明成员归属并限制整轮6000字符；RAG等待后读取当前世界，模型内部slot/流中重建隔离未实现。各系统已接入和仍缺的返回行动见[生态矩阵](hd2d-m6-integration-matrix.md)。

08:18 前端现有renderer/store/API测试57/57通过，隔离构建241 modules通过，输出`output/hd2d-checkpoint-0818/ui-build`，仅既有混合导入/chunk体积提示。固定发型卡UI的375/740浏览器及9张截图已复核，见[UI证据](hd2d-b-workshop-bob-ui.md)。三类平面材质单遍已通过六场景完整framebuffer颜色、depth、shadow-map和拾取24组严格一致检查；浏览器呈现PNG自身仍有低位差异，原始失败与判据调整记录均保留。同进程独占ABBA单轮720帧：双遍A1/A2 CPU p95为7.8/8.4ms，单遍B1/B2为3.9/3.4ms，切回双遍开销回升；rAF仍约16.7ms，未宣称帧率提升。该独立fixture无Canvas标签层，不与历史总CPU拼接，完整数据及组间漂移见压力报告。

测试均使用内存/临时数据库、依赖注入或浏览器 fixture；没有启动默认业务后端、修改真实存档或调用真实模型/生图。实际服务测试拦截 ComfyUI 模块导入时的只读 schema 请求，返回空 fixture，避免网络及后台重试。

08:32前端59/59及隔离构建242 modules通过，产物`output/hd2d-checkpoint-0832/ui-build`。温度支持数值和原天气标签；[手动恢复HD2D](hd2d-renderer-manual-recovery.md)补齐contextlost后原档位直接重试，真实WebGL balanced/low验证双击单实例、0业务POST/0新增SSE及卸载晚响应不挂载。天气缓存的完整预报时间与原子刷新8项、实际启动1项和小镇同步读取3项通过，已计入08:34检查点；时间/季节使用town时区，未知天气不冒用旧小时。不宣称咖啡馆A链完成。

08:34检查点之后：[首次奇遇](hd2d-m6-event-life-context.md)仅在开场生成前读取主角色此前背景，5项及共享组合23项通过，分支/结局不改。天气专题四文件最终23/23通过，新增预报批次冲突/重叠拒绝、可选读取失败降级和NPC无作息时的town时区睡眠；日志`output/hd2d-weather-final-spec.log`。[雨天有限避雨](hd2d-m6-rain-shelter.md)20项runtime通过，独立真实业务优先级1项通过；已计入09:04完整检查点。仅rules模式纯idle回自己明确住所，每预报时段最多15分钟持久等待预算，中断/换家不重置；工作、服务、预约和原安排优先。居民近况新增用途说明，浏览器20次只读请求回归通过；不宣称完整咖啡馆A链完成。

最后一轮天气边界补齐：[天气事实](hd2d-m6-weather-facts-plan.md)按完整UTC有效区间及配置来源读取；切城市、A→B→A和同城乱序回包由revision/generation隔离，原配置setter实际模块测试使用内存库。[调度器生命周期](hd2d-weather-scheduler-lifecycle.md)修复延迟启动中stop/restart遗留轮询，6项通过；停止调度不代表取消在途HTTP。避雨计划包含完整sourceKey，切城市同小时同雨也取消旧移动/等待，原15分钟预算不重置；三文件36/36已计入最终663检查点。

## 下一次继续的位置

1. 按原计划10.3节A链补咖啡馆明确经营实体、有限饮品库存/配方及有预算的补货触发，先给出与现有工坊/配送账本的来源契约，再接运行层；当前回家避雨不能直接推导消费或库存减少。[下一切片交接](hd2d-m6-cafe-next-slice.md)已定位固定工坊slice/配方边界和实施门槛，尚未实现咖啡馆交易。
2. 按[生态矩阵](hd2d-m6-integration-matrix.md)补正式生活帖/活动的幂等来源和明确接受入口，复用已结算事实；已有只读上下文不解析为奖励或预约。
3. 沿[长期回放](hd2d-m7-long-run.md)保留正常/囤币反例，继续验收有限资源耗尽、商家收入和无垫资任务可得性；不以余额非负代替经济平衡。
4. 完成安卓真机、真实存档人工操作和最终素材校准；桌面fixture与单遍材质CPU样本不能替代这些证据。

## 尚不能宣称完成

普通 M3 工作动作只提供可恢复工时事实，只有符合 M4 批次的双人证明才实际结算工资。M6 完整外部联动、M7 全镇压力和最终美术尚未整体验收；有限补助不能解决有限资源与囤钱路径的全部停滞，长期经济平衡仍未解决。安卓真机、真实存档人工操作与商业画面完成度不由桌面 fixture 代替。
